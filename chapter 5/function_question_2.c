#include <stdio.h>
#include <math.h> //including math.h library
int main() {
    int a;
    printf("enter length of side ");
    scanf("%d", &a);
    printf("area = %f", pow(a,2)); //pow= power function
    return 0;
}
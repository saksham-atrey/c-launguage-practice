#include <stdio.h>

int main() {
    for(int i= 5;i;i--){  //value of i is being used for condition i.e non zero value is true and 0 value is false
        printf("%d\n", i);
    }

    return 0;
}